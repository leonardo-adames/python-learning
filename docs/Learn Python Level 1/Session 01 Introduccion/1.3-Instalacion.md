# **Instalar Python**

Para instalar Python en un **sistema Windows**, podemos descargar el instalador desde el<br>
[sitio web oficial de Python](https://www.python.org/downloads/).

Una vez descargado el instalador, simplemente ejecutamos el archivo y seguimos las instrucciones del asistente de instalación **IMPORTANTE MARCAR TODAS LAS OPCIONES COMO SE VE EN LA IMAGEN ACONTINUACIÓN, PRINCIPALMENTE LA OPCIÓN DE `INSTALAR PIP` & `AGREGAR PYTHON AL PATH`**.


<div align="center">
<br>
    <img src="https://www.luisllamas.es/images/20437/python-installer-windows.webp" />
</div>
<br>
<br>

# **Verificar la instalación**

Una vez que la instalación haya finalizado, es conveniente verificar que Python se haya instalado correctamente en tu sistema.

Para ello, abrimos una ventana de terminal y escribimos el siguiente comando,

con el que verificaremos la versión de Python instalada:
```console
python --version
```
Este comando debería imprimir la versión de Python instalada en tu sistema. Si ves algo como:

```console
Python 3.11.0
```

Donde `3.X.X` es la versión de Python **¡felicidades!** Has instalado Python correctamente en tu sistema 🥳, Entonces Python se ha instalado correctamente.

# Entornos de desarrollo

Ahora que tenemos Python instalado en nuestro sistema, necesitamos un entorno de desarrollo donde podamos escribir, depurar y ejecutar nuestro código.

Existen varios entornos de desarrollo populares para Python, cada uno con sus propias características y ventajas.

<div align="center">
<br>
    <img src="https://www.luisllamas.es/images/20437/python-idle.webp" />
</div>
<br>

`IDLE` es muy muy básico, y solo lo recomiendo para las tareas más sencillas, o para un par de pruebas rápidas. Lo normal es que usemos un IDE más completo como `VS Code`, `PyCharm` o `Windsurf` o `Antigrabty` de **Google** Moderno y con implementación de `inteligencia Artificial` 👇.